import { ShowContentDirective } from './show-content.directive';

describe('ShowContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowContentDirective();
    expect(directive).toBeTruthy();
  });
});
